export default defineEventHandler((event) => {
    // const eligible = false

    // if (event.node.req.url.indexOf("/api/buy/*") > -1 && !user) {
    //     throw new Error("Not Eligible")
    // }
})