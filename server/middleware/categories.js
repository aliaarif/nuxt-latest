export default defineEventHandler((event) => {
    const user = false

    // if (event.node.req.url.indexOf("/api") > -1 && !user) {
    //     throw new Error("Not Authenticated")
    // }
    // console.log('New Request: ', + event.node.req.url)
})